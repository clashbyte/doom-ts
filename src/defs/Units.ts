export const SCALE = 1.0 / 64;

export const PLAYER_WALK_ACCEL = (30 * SCALE) / 60;
export const PLAYER_RUN_ACCEL = (55 * SCALE) / 60;
export const PLAYER_WALK_SPEED = (291.66 * SCALE) / 60;
export const PLAYER_RUN_SPEED = (583.33 * SCALE) / 60;

export const PLAYER_VIEW_HEIGHT = 41 * SCALE;
